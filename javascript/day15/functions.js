function add(a, b) {
    return a + b
}

function multiply(c, d) {
    return c * d;
}

const myCourse = "Web Design"

// module.exports = { add, multiply, myCourse }
// named export

module.exports = myCourse;
// default export
